Put custom certificate fonts in this folder.

Supported file types:

- `.ttf`
- `.otf`
- `.woff`
- `.woff2`

After adding fonts, refresh the Vite dev server page. For a production build, run:

```bash
npm run build
```

The website will list the fonts automatically in the font selectors.

